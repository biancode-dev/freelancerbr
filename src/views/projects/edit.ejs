<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Projeto - FreelancerBrasileiro</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <!-- Inclusão do Header -->
    <%- include('../partials/header') %>

        <main>
            <div class="container">
                <h1>Editar Projeto</h1>

                <!-- Formulário de Edição de Projeto -->
                <form action="/projects/<%= project.id %>/edit" method="POST">
                    <!-- Campo para o título do projeto -->
                    <div class="form-group">
                        <label for="title">Título do Projeto</label>
                        <input type="text" id="title" name="title" value="<%= project.title %>" required
                            placeholder="Ex: Criar um site para loja online">
                    </div>

                    <!-- Campo para descrição do projeto -->
                    <div class="form-group">
                        <label for="description">Descrição do Projeto</label>
                        <textarea id="description" name="description" required
                            placeholder="Ex: Desenvolver um site responsivo e moderno para loja..."><%= project.description %></textarea>
                    </div>

                    <!-- Campo para valor do projeto -->
                    <div class="form-group">
                        <label for="value">Valor (R$)</label>
                        <input type="number" id="value" name="value"
                            value="<%= typeof project.value === 'number' ? project.value.toFixed(2) : '' %>" required
                            step="0.01" placeholder="Ex: R$ 1000,00">
                    </div>

                    <!-- Botão de Salvar Alterações -->
                    <div class="form-actions">
                        <button type="submit" class="btn">Salvar Alterações</button>
                    </div>
                </form>

                <!-- Botão para voltar para a lista de projetos -->
                <div class="buttons-container">
                    <a href="/projects" class="btn">Voltar para Lista de Projetos</a>
                </div>
            </div>
        </main>

        <!-- Inclusão do Footer -->
        <%- include('../partials/footer') %>
</body>

</html>